services:
  postgres:
    container_name: condors-postgres
    image: postgres:16.5-alpine3.20
    environment:
      - POSTGRES_USER=pg
      - POSTGRES_PASSWORD=pw
      - POSTGRES_DB=db
    ports:
      - "5211:5432"
    restart: always
